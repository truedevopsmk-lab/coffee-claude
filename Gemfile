source "https://rubygems.org"

# Mirrors the GitHub Pages build environment so local previews match production.
gem "github-pages", group: :jekyll_plugins

# Faster local rebuilds on macOS.
gem "webrick", "~> 1.8"
